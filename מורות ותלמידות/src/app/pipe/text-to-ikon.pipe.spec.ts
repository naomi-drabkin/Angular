import { TextToIkonPipe} from './text-to-ikon.pipe';

describe('TextToIkonPipe', () => {
  it('create an instance', () => {
    const pipe = new TextToIkonPipe();
    expect(pipe).toBeTruthy();
  });
});
